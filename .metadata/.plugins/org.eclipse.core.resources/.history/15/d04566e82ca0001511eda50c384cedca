package Entity.Mob;

import Entity.Entity;

public abstract class Mob extends Entity {
	protected boolean Moving = false;

	public void Move(int xa, int ya) {
		if (xa > 0)
			setDir(1);
		if (xa < 0)
			setDir(3);
		if (ya > 0)
			setDir(2);
		if (ya < 0)
			setDir(0);
		if (getX() < 1379.0 && getX() > 26.0)
			setX((getX() + xa));
		if (getY() < 1377 && getY() > 32.0)
			setY((getY() + ya));
	}

	public void Update() {
	}

	public void Render() {
	}
}